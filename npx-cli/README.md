# JB AI Vibe Kanban

Vibe Kanban project was super cool; I forked it and adopted it for internal purposes.

Run instantly:

```bash
npx jbai-vibe-kanban
```

Install and run:

```bash
npm install jbai-vibe-kanban
npx jbai-vibe-kanban
```

Start a project (inside the app):

```text
Create Project → Single repository or Folder with repositories
```
